export enum Tags {
  div = 'div',
  img = 'img',
  btn = 'button',
  tpl = 'template',
  input = 'input',
  span = 'span',
  label = 'label',
  select = 'select',
  option = 'option',
}

export enum Colors {
  white = 'белый',
  yellow = 'желтый',
  red = 'красный',
  blue = 'синий',
  green = 'зелёный',
}

export enum Shapes {
  ball = 'шар',
  bell = 'колокольчик',
  toy = 'фигурка',
  pine = 'шишка',
  snowflake = 'снежинка',
  star = 'звезда',
}

export enum Sizes {
  S = 'малый',
  M = 'средний',
  L = 'большой',
}
